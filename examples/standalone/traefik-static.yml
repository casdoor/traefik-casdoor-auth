# Traefik Static Configuration for Standalone ForwardAuth Mode
entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

api:
  dashboard: true
  insecure: true

providers:
  file:
    filename: /etc/traefik/traefik-dynamic.yml
    watch: true

log:
  level: INFO
